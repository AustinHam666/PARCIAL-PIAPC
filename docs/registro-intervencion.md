# Registro de intervencion agentica

Registra cada ciclo relevante de herramienta. No copies razonamientos internos del modelo ni datos sensibles.

| Fecha o version | Instruccion resumida | Accion o herramienta | Resultado observable | Decision humana |
|---|---|---|---|---|
| 2026-09-15 | Definir motor, nombre de proyecto, autor y comportamiento a resolver; completar GDD.md | Preguntas al estudiante + edicion de `GDD.md`, `README.md`, `docs/especificacion.md`, `docs/plan.md`, `docs/matriz-permisos.md`, `docs/auditoria-repositorio.md`, `docs/evidencia-pruebas.md` | Documentacion base completa para iniciar el desarrollo del movimiento de paletas (Pong) | Aceptar, con correccion: el estudiante corrigio el eje de movimiento de las paletas de horizontal a vertical en el GDD |
| 2026-09-15 | Crear proyecto Unity 2022.3.62f3 en Unity Hub, consolidar carpeta con la documentacion | Comandos `mv` para unificar `README.md`, `GDD.md`, `AGENTS.md`, `.gitignore`, `docs/` dentro de `Parcial-PIAPC/`; reemplazo de `.gitignore` por el oficial de Unity | Repositorio unico con proyecto Unity + documentacion en la raiz | Aceptar |
| 2026-09-15 | Iniciar git y hacer el primer commit local (Paso 1-2 del plan) | `git init`, `git add -A`, `git commit` | Commit `f3ceb0f` con proyecto Unity vacio + documentacion, sin carpetas generadas (Library/Temp/Logs/UserSettings excluidas) | Aceptar |
| 2026-09-15 | Generar por codigo las paletas en la escena (Paso 3 del plan) | Edicion directa de `Assets/Scenes/SampleScene.unity` (YAML): GameObjects `PaddleLeft`/`PaddleRight` con Transform, SpriteRenderer y BoxCollider2D | El estudiante abrio el proyecto en el Editor, confirmo 0 errores en consola y ambas paletas visibles en la vista Game (captura de pantalla) | Aceptar |
| 2026-09-15 | Commitear y pushear cada paso; autorizacion para usar red y publicar en el remoto | `git remote add origin`, `git fetch`, `git checkout -b PARCIAL-1`, `git commit`, `git push -u origin PARCIAL-1` | Rama `PARCIAL-1` creada y pusheada a `https://github.com/AustinHam666/PARCIAL-PIAPC`, sin tocar `main` (que tiene una historia distinta de la plantilla) | Aceptar: el estudiante autorizo explicitamente commit + push por cada paso, en la rama `PARCIAL-1` |
| 2026-09-15 | Pasos 4 y 6 del plan: crear `PaddleMovement.cs` y asignarlo a cada paleta con su mapeo de teclas | Creacion de `Assets/Scripts/PaddleMovement.cs` (+ `.meta`), edicion de `SampleScene.unity` para agregar el componente `MonoBehaviour` a `PaddleLeft` (W/S) y `PaddleRight` (flechas arriba/abajo) | Commit y push a `PARCIAL-1` pedidos por el estudiante inmediatamente despues del cambio; la verificacion visual en Play Mode (movimiento fluido, sin errores) queda pendiente de confirmacion del estudiante en el proximo ciclo | Aceptar (commit y push realizados a pedido explicito); verificacion funcional pendiente |
| 2026-09-15 | Confirmacion: el movimiento funciona bien; pedido de agrandar las paletas. Continuar con Paso 5 (limites de cancha) | Escala de paletas aumentada de (0.4, 1.5, 1) a (0.5, 2.5, 1) en `SampleScene.unity`; se agrego clamping `Mathf.Clamp(position.y, minY, maxY)` en `PaddleMovement.cs` con `minY=-3.75`, `maxY=3.75` (calculado segun media altura de camara ortografica 5 menos media altura de la paleta agrandada) | Pendiente de verificacion visual del estudiante en Play Mode (paleta no debe salir de pantalla ni vibrar en los bordes) | Aceptar (confirmacion de movimiento) y ejecutar mejora + paso 5 a pedido del estudiante |
| 2026-09-15 | Confirmacion: los limites de los bordes estan bien. Pedido: agrandar mas las paletas, ponerlas blancas, fondo negro, y marcar los bordes de cancha con una linea, por motivos visuales | Escala de paletas aumentada de (0.5, 2.5, 1) a (0.6, 3.5, 1); `minY`/`maxY` recalculados a -3.25/3.25 en ambos `PaddleMovement`; color de paleta ya era blanco (`m_Color: 1,1,1,1`), se mantiene; `Main Camera.m_BackGroundColor` cambiado a negro `{0,0,0,1}`; se agregan dos GameObjects visuales `BorderTop` y `BorderBottom` (SpriteRenderer blanco, sin collider) en y=+4.8/-4.8 marcando el limite superior e inferior de cancha | Confirmado por el estudiante: "anda bien" | Aceptar |
| 2026-09-15 | Confirmacion final del estudiante sobre las mejoras visuales (paletas grandes, fondo negro, bordes marcados) | Ninguna (no habia cambios pendientes; se verifico `git status` limpio en `faa32da`) | Sin cambios que commitear; se deja constancia de la confirmacion | Aceptar |
| 2026-09-15 | Cerrar Etapa 1 (movimiento de paletas) con informe final y continuar con los siguientes pasos | Se completo `docs/informe-final.md` (Etapa 1) y se agrego la Etapa 2 (pasos 7-12: pelota, rebotes, puntaje) a `docs/plan.md` | Documentacion de cierre de etapa lista; Etapa 2 planificada y pendiente de implementar | Aceptar |
| 2026-09-15 | Paso 7 del plan: crear GameObject `Ball` en el centro de la cancha, sin logica aun | Edicion de `SampleScene.unity`: GameObject `Ball` en (0,0,0), escala 0.4, `SpriteRenderer` (sprite redondo built-in), `Rigidbody2D` (gravityScale 0, bodyType Dynamic) y `CircleCollider2D` | Pendiente de verificacion visual del estudiante en el Editor (pelota visible en el centro, sin caer por gravedad) | Pendiente de confirmacion |
| 2026-09-15 | Correccion de error reportado por el estudiante: "PPtr cast failed when dereferencing! Casting from Texture2D to Sprite at FileID 10906!" | Se reemplazo la referencia `m_Sprite` del GameObject `Ball` de `fileID: 10906` (Texture2D, referencia invalida) a `fileID: 10907` (Sprite valido ya usado en paletas y bordes) en `SampleScene.unity` | Pendiente de confirmacion del estudiante de que el error desaparecio | Corregido a partir del error reportado; pendiente de confirmacion |
| 2026-09-15 | Pedido: agrandar la pelota | Escala de `Ball` aumentada de (0.4, 0.4) a (0.8, 0.8) en `SampleScene.unity`; el `CircleCollider2D` (radio local 0.5) escala junto con el Transform | Confirmado por el estudiante: "bien" | Aceptar |
| 2026-09-15 | Pedido: agrandar la pelota al doble o mas | Escala de `Ball` aumentada de (0.8, 0.8) a (1.6, 1.6) en `SampleScene.unity` | Confirmado por el estudiante: "bien" | Aceptar |
| 2026-09-15 | Paso 8 del plan: `BallMovement.cs` con velocidad constante en direccion inicial oblicua, sin rebote aun | Creacion de `Assets/Scripts/BallMovement.cs` (+ `.meta`) usando `Rigidbody2D.velocity`; direccion inicial aleatoria en X (izquierda o derecha) con leve componente en Y (`Random.Range(-0.5, 0.5)`) normalizada, para evitar trayectorias puramente horizontales o verticales; componente asignado al `Ball` en `SampleScene.unity` con `speed=7` | Confirmado a pedido del estudiante de continuar (commit y push hechos sin observaciones adicionales) | Aceptar |
| 2026-09-15 | Paso 9 del plan: agregar collider a los bordes y logica de rebote vertical de la pelota | Se crea `Assets/Physics/BallBounce.physicsMaterial2D` (friction 0, bounciness 1); se asigna al `Rigidbody2D` de `Ball`; se agrega `BoxCollider2D` (con el mismo material) a `BorderTop` y `BorderBottom`, que antes eran solo visuales | Pendiente de verificacion visual del estudiante en Play Mode (la pelota debe rebotar contra el borde superior e inferior sin atravesarlo; todavia atraviesa las paletas hasta el paso 10) | Pendiente de confirmacion |
| 2026-09-15 | Correccion de error reportado por el estudiante: "Can't produce abstract class Collider2D" | El material fisico se habia asignado por error al campo `m_Material` del `Rigidbody2D` de `Ball` (ese campo ahi no corresponde a un material fisico de collider en Unity 2022.3). Se movio la referencia al `m_Material` del `CircleCollider2D` de `Ball`, que es el lugar correcto para asignar un `PhysicsMaterial2D` | El estudiante reporto que el mismo error persistia | No corregido; se aplico un enfoque distinto en el siguiente ciclo |
| 2026-09-15 | El error "Can't produce abstract class Collider2D" persistio tras la primera correccion | Se elimino por completo el asset `Assets/Physics/BallBounce.physicsMaterial2D` (creado a mano en YAML, posible causa de la corrupcion de referencias) y todas sus referencias `m_Material` en `SampleScene.unity` (vuelven a `{fileID: 0}`); se reemplazo el rebote fisico automatico por logica programada en `BallMovement.cs` (`OnCollisionEnter2D` con `Vector2.Reflect` sobre la normal de contacto), que no depende de ningun asset nativo escrito a mano | Confirmado por el estudiante: "anda" | Aceptar |
| 2026-09-15 | Pedido: alargar/engrosar las lineas de borde para que se entiendan mejor las colisiones. Continuar con los pasos siguientes del plan | Escala de `BorderTop`/`BorderBottom` aumentada de (18, 0.15) a (20, 0.3) en `SampleScene.unity` (el `BoxCollider2D` escala junto con el Transform, mejorando tambien la deteccion de colision) | Pendiente de confirmacion visual del estudiante | Aceptar, a pedido explicito |
| 2026-09-15 | Paso 10 verificado sin cambios de codigo: rebote de la pelota contra las paletas | Las paletas ya tenian `BoxCollider2D` desde el paso 3; la logica `OnCollisionEnter2D` de `BallMovement.cs` (agregada en el paso 9) es generica y reacciona contra cualquier collider, incluidas las paletas, sin necesitar codigo adicional | Pendiente de confirmacion visual del estudiante en Play Mode | Aceptar (verificacion, sin cambios) |
| 2026-09-15 | Pasos 11 y 12 del plan: deteccion de punto y reinicio de la pelota | `BallMovement.cs`: se agrega `courtHalfWidth`, chequeo en `Update()` de si la pelota cruzo el limite izquierdo o derecho de la cancha, llamado a `GameManager.Instance.RegisterPoint(...)` y `ResetBall()` (vuelve al centro y relanza en direccion aleatoria). Se crea `Assets/Scripts/GameManager.cs` con `leftScore`/`rightScore` (visibles en el Inspector, sin UI en pantalla todavia) y se agrega el GameObject `GameManager` a la escena. El contador visual en pantalla (UI Text/TMP) se deja para un paso guiado en el Editor, evitando repetir el problema de serializacion manual ya sufrido con el PhysicsMaterial2D | Confirmado por el estudiante: "anda" | Aceptar |
| 2026-09-15 | Reporte del estudiante: pantalla en Play Mode muestra las lineas de borde cortas/redondeadas que no llegan a cubrir arriba/abajo, y las colisiones "se sienten raras" (bote antes o despues de donde se ve la linea). Pedido: alargar lineas, mostrar puntajes en pantalla, reiniciar partida al llegar a 10 puntos, agregar particulas a la pelota y una explosion al anotar gol | Se identifico que el sprite built-in usado para las lineas tiene esquinas redondeadas/degrade que se notan al estirarlo muy fino, haciendo que el collider (invisible, recto de punta a punta) no coincida visualmente con el sprite (visible, mas corto). Se aumento el ancho de `BorderTop`/`BorderBottom` de 20 a 40 para que los extremos redondeados queden fuera de camara y se vea una linea recta de punta a punta, resolviendo tambien la sensacion de colision "rara". | Se actualizo `GameManager.cs`: agrega `scoreToWin=10`, campos `leftScoreText`/`rightScoreText` (Text, para asignar en Inspector una vez creado el Canvas), `goalExplosionPrefab` (para asignar un prefab de particulas), `RegisterPoint` ahora recibe la posicion del gol y dispara la explosion si hay prefab asignado, y reinicia el marcador + la pelota si algun jugador llega a `scoreToWin`. `BallMovement.ResetBall()` se paso a publico y ahora se llama tambien desde `GameManager.ResetMatch()`. Se conecto el campo `ball` del `GameManager` en la escena al `BallMovement` de `Ball`. No se agrego el `TrailRenderer` de la pelota ni el Canvas/UI por edicion manual de YAML, para no repetir el riesgo de corromper la escena (ver fila siguiente sobre el error de Collider2D persistente); esos pasos quedan pendientes de forma guiada en el Editor | Pendiente de confirmacion visual (marcador aun no visible en pantalla, falta que el estudiante cree el Canvas/Text y lo asigne) |
| 2026-09-15 | El estudiante compartio una captura donde la linea seguia viendose corta pese al cambio a ancho 40, y la consola mostraba 3 errores; se sospecho cache local corrupta de Unity | Se confirmo que el archivo `SampleScene.unity` en disco ya tenia el valor correcto (ancho 40); se explico que el Editor probablemente tenia la escena vieja cargada en memoria sin releer el archivo. Se recomendo cerrar Unity, borrar la carpeta `Library/` (no versionada, se regenera sola) y reabrir el proyecto para forzar reimportacion completa | El estudiante borro `Library/`, reabrio el proyecto y confirmo: "anda... el error ya no esta". El error "Can't produce abstract class Collider2D" quedo resuelto (causa raiz: cache corrupta de Unity, no el contenido de la escena) | Aceptar; sin cambios de codigo necesarios en este ciclo |
| 2026-09-15 | Pedido: marcador visible en pantalla, reinicio de partida a 10 puntos (ya implementado en el ciclo anterior), particulas en la pelota y explosion al anotar gol. El estudiante pidio que se implemente todo sin guiarlo en el Editor | Se opto por implementar los tres efectos enteramente en codigo C#, evitando Canvas/UI y ParticleSystem nativos de Unity (los componentes mas complejos de serializar a mano, causa raiz del incidente anterior). Se reemplazo `GameManager.cs`: elimina los campos `leftScoreText`/`rightScoreText`/`goalExplosionPrefab` y agrega `OnGUI()` que dibuja el puntaje de cada jugador arriba de la pantalla con `GUI.Label`. Se crean `FadeAndDestroy.cs` (helper que escala y desvanece un `SpriteRenderer` hasta destruirse), `GoalExplosionEffect.cs` (clase estatica que genera 14 fragmentos amarillos que salen disparados desde el punto del gol y se desvanecen) y `BallTrailEffect.cs` (genera copias fantasma semitransparentes de la pelota cada 0.04s que se desvanecen, simulando una estela de particulas). Se agrega `BallTrailEffect` como componente de `Ball` en la escena y se simplifica el `GameManager` en `SampleScene.unity` (ya no tiene campos de UI/prefab sin asignar) | Pendiente de verificacion visual del estudiante en Play Mode (numeros de puntaje arriba de la pantalla, estela detras de la pelota, explosion amarilla al anotar) | Aceptar, a pedido explicito ("hacelo todo vos") |

## Correcciones y acciones rechazadas

- El GDD inicial describia paletas moviendose en el eje horizontal; el estudiante corrigio a Pong clasico (paletas a los costados, movimiento vertical) al elegir el mapeo de teclas W/S y flechas arriba/abajo. Se corrigio `GDD.md` y `docs/especificacion.md` en consecuencia.
